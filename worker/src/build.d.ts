/**
 * The commit this bundle was built from, replaced at build time by the program that
 * deploys it. Declared rather than imported: there is no module to import it from,
 * because it does not exist until the bundler substitutes it.
 */
declare const __COMMIT__: string;
