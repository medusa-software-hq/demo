name: Check frontend

on:
  workflow_call:

permissions:
  contents: read

env:
  NO_COLOR: 1

jobs:
  check-frontend:
    name: Check frontend source
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - uses: actions/setup-node@v7.0.0
        with:
          node-version-file: frontend/.nvmrc

      # The app imports `demo-client` from next door, and half of that package is generated
      # from the contract rather than committed. Installing the app links that package but
      # cannot fill in the missing half, so it is generated first — `tsc` and `vite` both
      # follow imports rather than the gitignore.
      - name: Install the client's dependencies
        working-directory: api/client
        run: npm ci

      - name: Generate the wire client
        working-directory: api/client
        run: npm run generate

      - name: Install dependencies
        run: npm ci

      - name: Run `oxfmt`
        run: npx --no-install oxfmt --check "**/*.{ts,tsx,css}"

      - name: Run `oxlint`
        # `--type-aware` is where the rules that need the type checker live.
        run: npx --no-install oxlint --type-aware

      - name: Run `tsc`
        # Two programs: the app runs in a browser, the build configuration runs in Node.
        run: |
          npx --no-install tsc --noEmit -p tsconfig.app.json
          npx --no-install tsc --noEmit -p tsconfig.node.json

      - name: Run `vite build`
        # The deployment builds this too, but only once it has credentials and a stack
        # to plan against. Here it is the cheapest thing that can fail, and it fails on
        # its own.
        run: npm run build
